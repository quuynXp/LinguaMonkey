name: LinguamonkeyFe - Signed APK Build (Final SDK Fix)

on:
  push:
    paths:
      - "LinguamonkeyFe/**"
    branches:
      - master
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./LinguamonkeyFe

    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4

      - name: 🗑️ AGGRESSIVE DISK CLEANUP
        run: |
          echo "🔥 Freeing up disk space..."
          # Xóa .NET (Rất nặng, không dùng cho React Native)
          sudo rm -rf /usr/share/dotnet
          # Xóa CodeQL
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          # Xóa Docker images
          sudo docker image prune --all --force
          # Xóa cache apt
          sudo apt-get clean
          # Xóa thư mục tmp
          sudo rm -rf /tmp/*
          
          # QUAN TRỌNG: KHÔNG ĐƯỢC XÓA /usr/local/lib/android (đây là SDK)
          # Chỉ xóa system-images của Android (nặng và chỉ dùng cho Emulator) để tiết kiệm chỗ
          sudo rm -rf /usr/local/lib/android/sdk/system-images
          
          echo "✅ Disk space cleaned."

      - name: 🧩 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'
          cache-dependency-path: ./LinguamonkeyFe/yarn.lock

      - name: 📦 Install dependencies
        run: yarn install --frozen-lockfile

      # --- 2. XÓA ANDROID CŨ ĐỂ PREBUILD LẠI TỪ ĐẦU ---
      - name: 💣 Delete existing Android folder
        run: rm -rf android

      - name: 🛠️ Generate Android directory (Prebuild)
        run: npx expo prebuild --platform android --no-install

      # --- 3. XÓA FILE WEBP (Tránh lỗi Duplicate Resource) ---
      - name: 🧹 Clean up duplicate icon files
        run: |
          echo "✨ Cleaning up duplicate icon files (removing .webp)..."
          if [ -d "./android/app/src/main/res" ]; then
            find ./android/app/src/main/res -name "*.webp" -delete
          fi

      - name: ☕ Setup Java (JDK)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # --- 4. CÀI ĐẶT NDK 26.1 (r26b) ---
      - name: 🔧 Setup Android NDK r26b
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26b
          add-to-path: true

      - name: 🐘 Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-fe-v3-${{ hashFiles('LinguamonkeyFe/**/*.gradle*', 'LinguamonkeyFe/**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-fe-v3-

      - name: 🔐 Setup Keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > ./android/app/my-release-key.keystore

      - name: ⚙️ Build APK with Gradle
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_STORE_FILE: my-release-key.keystore
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          cd android
          chmod +x gradlew
          echo "🔨 Building with NDK at: $ANDROID_NDK_HOME"
          
          # Dùng --no-daemon để ổn định memory
          ./gradlew assembleRelease --no-daemon \
            -Pandroid.injected.signing.store.file=$(pwd)/app/my-release-key.keystore \
            -Pandroid.injected.signing.store.password=${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.ANDROID_KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.ANDROID_KEY_PASSWORD }}

      - name: 📤 Upload signed APK
        uses: actions/upload-artifact@v4
        with:
          name: android-signed-apk
          path: LinguamonkeyFe/android/app/build/outputs/apk/release/app-release.apk

      - name: 🧹 Final Clean Up
        if: always()
        run: rm -f ./android/app/my-release-key.keystore