name: LinguamonkeyFe - Signed APK Build (Integration Fix)

on:
  push:
    paths:
      - "LinguamonkeyFe/**"
    branches:
      - master
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./LinguamonkeyFe

    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4

      # --- 1. SETUP JAVA & NODE ---
      - name: ☕ Setup Java (JDK)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🧩 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          # Tạm thời KHÔNG cache để tránh lỗi artifact cũ
          # cache: 'yarn' 

      # --- 2. CÀI NDK 26 ---
      - name: 🔧 Setup Android NDK r26b
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26b
          add-to-path: false 

      # --- 3. FIX NDK: THAY THẾ VẬT LÝ ---
      # Giữ nguyên bước này vì nó đã chứng minh hiệu quả chặn lỗi CMake
      - name: 🔄 SWAP SYSTEM NDK
        run: |
          echo "🧹 Wiping System NDK folder..."
          sudo rm -rf $ANDROID_HOME/ndk
          sudo mkdir -p $ANDROID_HOME/ndk
          
          echo "🔗 Linking NDK 26 into System Path..."
          sudo ln -s ${{ steps.setup-ndk.outputs.ndk-path }} $ANDROID_HOME/ndk/26.1.10909125
          
          echo "✅ NDK Linked."
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/26.1.10909125" >> $GITHUB_ENV
          echo "NDK_HOME=$ANDROID_HOME/ndk/26.1.10909125" >> $GITHUB_ENV

      # --- 4. PREPARE PROJECT & FIX DEPENDENCIES ---
      - name: 📦 Install dependencies
        run: yarn install --frozen-lockfile

      # BƯỚC MỚI: Cập nhật các module Expo để tương thích Gradle 8.7
      - name: 🚑 Fix Expo Dependencies
        run: |
          echo "🛠️ Running expo install --fix to align versions..."
          npx expo install --fix
          
          # Cài lại dependencies sau khi fix để chắc chắn
          yarn install

      - name: 💣 Delete existing Android folder
        run: rm -rf android

      - name: 🛠️ Generate Android directory (Prebuild)
        run: npx expo prebuild --platform android --no-install

      # --- 5. FIX GRADLE WRAPPER (8.7) ---
      - name: 🔧 Fix Gradle Wrapper (Set to 8.7)
        run: |
          echo "📝 Fixing Gradle Wrapper to version 8.7..."
          cd android/gradle/wrapper
          
          # Ghi đè file properties để trỏ về đúng bản 8.7 trên server gốc
          echo "distributionBase=GRADLE_USER_HOME" > gradle-wrapper.properties
          echo "distributionPath=wrapper/dists" >> gradle-wrapper.properties
          echo "distributionUrl=https\://services.gradle.org/distributions/gradle-8.7-bin.zip" >> gradle-wrapper.properties
          echo "zipStoreBase=GRADLE_USER_HOME" >> gradle-wrapper.properties
          echo "zipStorePath=wrapper/dists" >> gradle-wrapper.properties
          
          cat gradle-wrapper.properties

      - name: 📍 FORCE local.properties & Clean Cache
        run: |
          # Ghi đè local.properties
          echo "ndk.dir=$ANDROID_HOME/ndk/26.1.10909125" >> ./android/local.properties
          echo "sdk.dir=$ANDROID_HOME" >> ./android/local.properties
          
          # Xóa cache build cũ
          rm -rf ./node_modules/react-native-worklets/android/.cxx
          rm -rf ./android/.gradle
          rm -rf ~/.gradle/caches

      - name: 🧹 Clean up duplicate icon files
        run: |
          if [ -d "./android/app/src/main/res" ]; then
            find ./android/app/src/main/res -name "*.webp" -delete
          fi

      - name: 🔐 Setup Keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > ./android/app/my-release-key.keystore

      # --- 6. BUILD ---
      - name: ⚙️ Build APK with Gradle Wrapper
        env:
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_STORE_FILE: my-release-key.keystore
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ORG_GRADLE_PROJECT_MYAPP_UPLOAD_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          cd android
          chmod +x gradlew
          
          echo "🔨 Building with Gradle 8.7..."
          
          ./gradlew assembleRelease --no-daemon \
            -Pandroid.ndkVersion=26.1.10909125 \
            -PdownloadNdk=false \
            -Pandroid.injected.signing.store.file=$(pwd)/app/my-release-key.keystore \
            -Pandroid.injected.signing.store.password=${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.ANDROID_KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.ANDROID_KEY_PASSWORD }}

      - name: 📤 Upload signed APK
        uses: actions/upload-artifact@v4
        with:
          name: android-signed-apk
          path: LinguamonkeyFe/android/app/build/outputs/apk/release/app-release.apk

      - name: 🧹 Final Clean Up
        if: always()
        run: rm -f ./android/app/my-release-key.keystore